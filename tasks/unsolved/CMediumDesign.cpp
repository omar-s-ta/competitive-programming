#include <bits/stdc++.h>
using namespace std;

class CMediumDesign {
public:
  void solveOne(istream &is, ostream &os) {

  }

  void solve(istream &is, ostream &os) {
    int t = 1;
    for (int i = 0; i < t; i++) {
      solveOne(is, os);
    }
  }
};